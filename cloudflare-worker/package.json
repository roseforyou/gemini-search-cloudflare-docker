{
  "name": "gemini-search-cloudflare-worker",
  "version": "0.1.0",
  "description": "Cloudflare Browser Rendering worker for gemini-search-cloudflare-docker.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.5.4",
    "wrangler": "^3.80.0"
  }
}
